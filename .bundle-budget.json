{
  "$schema-comment": "Bundle size budgets for Dog Universe. Sizes are measured in KILOBYTES (1024 bytes), uncompressed. Numbers reflect what `next build` reports in the 'Size' column (NOT 'First Load JS', which adds shared chunks). Bump only with intentionality.",
  "limits": [
    {
      "name": "Framework + main shared chunks",
      "patterns": [
        ".next/static/chunks/main-*.js",
        ".next/static/chunks/webpack-*.js",
        ".next/static/chunks/framework-*.js",
        ".next/static/chunks/main-app-*.js"
      ],
      "maxKB": 560,
      "rationale": "React 19 + Next.js 15 runtime + App Router. Baseline ~520 KB uncompressed (~225 KB gzipped) as of Next 15.5 — matches what `next build` reports under 'First Load JS shared by all'. 40 KB headroom for natural growth. Bump only if a Next major version pushes baseline higher than 560 KB."
    },
    {
      "name": "Total JS shipped to client",
      "patterns": [".next/static/chunks/**/*.js"],
      "maxKB": 4500,
      "rationale": "Sum of every chunk. Bump only when adding a major feature (chart lib, PDF viewer, etc)."
    },
    {
      "name": "CSS bundle",
      "patterns": [".next/static/css/*.css"],
      "maxKB": 200,
      "rationale": "Tailwind purges aggressively; if this jumps you've probably leaked an unused class."
    }
  ]
}
